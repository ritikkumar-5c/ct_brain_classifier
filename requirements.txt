# Core
torch>=2.1
torchvision>=0.16
timm>=1.0.7            # MaxViT / ViT / TNT / ConvNeXt backbones (paper used timm)
pydicom>=2.4          # DICOM I/O
pyjpegls>=1.2         # JPEG-LS Lossless decoder (prepared CT data is JPEG-LS compressed)
pylibjpeg>=2.0        # plugin host
pylibjpeg-openjpeg>=2.0  # JPEG 2000 decoder (some series use it)
numpy>=1.24
scikit-learn>=1.3     # metrics (paper used sklearn)
pandas>=2.0
matplotlib>=3.7       # confusion matrix / ROC figures for TensorBoard
tensorboard>=2.15
tqdm>=4.66
Pillow>=10.0
